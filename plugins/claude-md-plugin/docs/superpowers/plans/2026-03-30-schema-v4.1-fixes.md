# Schema v4.1 Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 88399fd 커밋 리뷰에서 발견된 4개 결함 수정 — `is_project_root` 자동 감지 누락, `converge_schema` optional/conditional sections 미처리, 경고 메시지 하드코딩, `format_data_schemas` 누락

**Architecture:**
- Task 1: `evaluate_conditions`에 `.git` 감지 추가 → `is_project_root` 자동 판단
- Task 2: `converge_schema_with_context` 추가 → optional + conditional sections skeleton 생성
- Task 3: 경고 메시지 하드코딩 `"Flows"` → `name` 변수 (1줄 버그 수정)
- Task 4: `analysis_formatter`에 `format_data_schemas` 추가 → ExportedType/Enum → Data Schemas 섹션
- Task 5: Cucumber 시나리오 추가 — is_project_root 자동 감지 + converge optional + Flows 경고 수정

**Tech Stack:** Rust, cucumber-rs, tempfile

---

## File Map

| 파일 | 변경 유형 | Task |
|------|-----------|------|
| `core/src/schema_validator.rs` | Modify | 1, 2, 3 |
| `core/src/analysis_formatter.rs` | Modify | 4 |
| `core/tests/features/developers_md_validator.feature` | Modify | 5 |

---

### Task 1: evaluate_conditions에 .git 기반 is_project_root 감지 추가

**Files:**
- Modify: `core/src/schema_validator.rs` (evaluate_conditions 함수, ~line 56)

**배경:**
`evaluate_conditions`는 `is_module_root`만 설정하고 `is_project_root`는 항상 `false`로 남긴다.
주석("must be set by the caller")이 있으나 CLI caller도 설정하지 않아 Flows 조건이 전혀 작동하지 않는다.
`.git` 디렉토리 존재 여부가 project root의 가장 신뢰성 높은 마커이므로 이를 자동 감지에 사용한다.

- [ ] **Step 1: is_project_root=true 테스트 작성 (RED)**

`schema_validator.rs`의 `test_evaluate_conditions_module_root` 테스트 직후에 추가:

```rust
#[test]
fn test_evaluate_conditions_project_root() {
    let temp = TempDir::new().unwrap();
    std::fs::create_dir(temp.path().join(".git")).unwrap();
    let ctx = SchemaValidator::evaluate_conditions(temp.path());
    assert!(ctx.is_project_root);
    assert!(!ctx.is_module_root);
}

#[test]
fn test_evaluate_conditions_project_and_module_root() {
    let temp = TempDir::new().unwrap();
    std::fs::create_dir(temp.path().join(".git")).unwrap();
    File::create(temp.path().join("Cargo.toml")).unwrap();
    let ctx = SchemaValidator::evaluate_conditions(temp.path());
    assert!(ctx.is_project_root);
    assert!(ctx.is_module_root);
}
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
cd core && cargo test test_evaluate_conditions_project_root 2>&1 | tail -5
```

Expected: `FAILED` (assertion `ctx.is_project_root` fails)

- [ ] **Step 3: evaluate_conditions 구현 수정**

`schema_validator.rs`의 `evaluate_conditions` 함수를 다음으로 교체:

```rust
/// Evaluate conditions by scanning the given directory.
/// Detects module root by presence of build system markers (Cargo.toml, package.json, etc.).
/// Detects project root by presence of `.git` directory.
pub fn evaluate_conditions(dir: &Path) -> ValidationContext {
    let mut ctx = ValidationContext::default();
    ctx.is_module_root = MODULE_ROOT_MARKERS.iter().any(|m| dir.join(m).exists());
    ctx.is_project_root = dir.join(".git").exists();
    ctx
}
```

주석("must be set by the caller") 제거.

- [ ] **Step 4: 테스트 통과 확인**

```bash
cd core && cargo test test_evaluate_conditions 2>&1 | tail -10
```

Expected: 3개 테스트 모두 `ok`

- [ ] **Step 5: 커밋**

```bash
git add core/src/schema_validator.rs
git commit -m "fix(schema_validator): evaluate_conditions에 .git 기반 is_project_root 자동 감지 추가"
```

---

### Task 2: converge_schema_with_context — optional + conditional sections 처리

**Files:**
- Modify: `core/src/schema_validator.rs` (converge_schema + main.rs FixSchema handler)
- Modify: `core/src/main.rs` (FixSchema command handler, ~line 385)

**배경:**
현재 `converge_schema`는 required sections만 추가한다.
optional(allow_none=true) sections(Data Schemas)와 conditional sections(Flows, is_project_root)도
skeleton으로 추가해야 `/validate --auto-fix`와 `/migrate`가 완전한 교정을 수행한다.

`converge_schema(content, doc_type)` 시그니처를 유지하면서 context를 받는 오버로드를 추가한다.

- [ ] **Step 1: optional sections 테스트 작성 (RED)**

`schema_validator.rs`의 converge_schema 테스트 블록 끝에 추가:

```rust
#[test]
fn test_converge_developers_md_adds_optional_data_schemas() {
    let content = r#"# Test Module

## Constraints
None

## Technical Context
None
"#;
    let validator = SchemaValidator::new();
    let result = validator.converge_schema(content, "developers_md");

    assert!(
        result.content.contains("## Data Schemas"),
        "Expected Data Schemas to be added: {}", result.content
    );
    assert!(result.changes.iter().any(|c| c.contains("Data Schemas")));
}

#[test]
fn test_converge_developers_md_idempotent_with_data_schemas() {
    let content = r#"# Test Module

## Constraints
None

## Data Schemas
None

## Technical Context
None
"#;
    let validator = SchemaValidator::new();
    let result = validator.converge_schema(content, "developers_md");

    assert!(result.changes.is_empty(), "Should be idempotent: {:?}", result.changes);
}
```

- [ ] **Step 2: conditional(Flows) 테스트 작성 (RED)**

```rust
#[test]
fn test_converge_developers_md_adds_flows_at_project_root() {
    let temp = TempDir::new().unwrap();
    std::fs::create_dir(temp.path().join(".git")).unwrap();

    let content = r#"# Project Root

## Constraints
None

## Technical Context
None
"#;
    let validator = SchemaValidator::new();
    let ctx = SchemaValidator::evaluate_conditions(temp.path());
    let result = validator.converge_schema_with_context(content, "developers_md", Some(&ctx));

    assert!(
        result.content.contains("## Flows"),
        "Expected Flows to be added at project root: {}", result.content
    );
    assert!(result.changes.iter().any(|c| c.contains("Flows")));
}

#[test]
fn test_converge_developers_md_no_flows_at_non_root() {
    let temp = TempDir::new().unwrap();
    // no .git → not project root

    let content = r#"# Module

## Constraints
None

## Technical Context
None
"#;
    let validator = SchemaValidator::new();
    let ctx = SchemaValidator::evaluate_conditions(temp.path());
    let result = validator.converge_schema_with_context(content, "developers_md", Some(&ctx));

    assert!(
        !result.content.contains("## Flows"),
        "Flows should NOT be added at non-root: {}", result.content
    );
}
```

- [ ] **Step 3: 테스트 실패 확인**

```bash
cd core && cargo test test_converge_developers_md_adds_optional 2>&1 | tail -5
cd core && cargo test test_converge_developers_md_adds_flows 2>&1 | tail -5
```

Expected: 두 테스트 모두 `FAILED`

- [ ] **Step 4: converge_schema_with_context 구현**

`schema_validator.rs`에서 `converge_schema` 함수를 다음으로 교체:

```rust
/// Converge schema (no context — required sections only for backward compat).
pub fn converge_schema(&self, content: &str, doc_type: &str) -> ConvergeResult {
    self.converge_schema_with_context(content, doc_type, None)
}

/// Converge schema with optional context for conditional section handling.
/// - required sections: always added if missing (with None placeholder)
/// - optional allow_none sections (e.g. Data Schemas): added if missing
/// - conditional sections (e.g. Flows): added only when context condition is met
pub fn converge_schema_with_context(
    &self,
    content: &str,
    doc_type: &str,
    ctx: Option<&ValidationContext>,
) -> ConvergeResult {
    let mut result = ConvergeResult::default();
    let mut output = content.to_string();

    // Step 1: Renames
    for &(from, to, document) in MIGRATION_RENAMES {
        if document != doc_type {
            continue;
        }
        let sections = self.parse_sections(&output);
        let has_from = sections.iter().any(|s| s.name.eq_ignore_ascii_case(from));
        let has_to = sections.iter().any(|s| s.name.eq_ignore_ascii_case(to));

        if has_from && !has_to {
            output = Self::rename_section(&output, from, to);
            result.changes.push(format!("renamed: ## {} → ## {}", from, to));
        } else if has_from && has_to {
            result.warnings.push(format!(
                "conflict: both '## {}' and '## {}' exist — skipped rename, manual merge needed",
                from, to
            ));
        }
    }

    // Step 2: Removals
    for &(name, document) in MIGRATION_REMOVALS {
        if document != doc_type {
            continue;
        }
        let sections = self.parse_sections(&output);
        if sections.iter().any(|s| s.name.eq_ignore_ascii_case(name)) {
            output = Self::remove_section(&output, name);
            result.changes.push(format!("removed: ## {}", name));
        }
    }

    // Step 3: Add missing required sections
    let (required_sections, allow_none_sections) = if doc_type == "developers_md" {
        (DEVELOPERS_REQUIRED_SECTIONS, DEVELOPERS_ALLOW_NONE_SECTIONS)
    } else {
        (REQUIRED_SECTIONS, ALLOW_NONE_SECTIONS)
    };

    let sections = self.parse_sections(&output);
    for required in required_sections {
        let found = sections.iter().any(|s| s.name.eq_ignore_ascii_case(required));
        if found {
            continue;
        }
        let allows_none = allow_none_sections.iter().any(|s| s.eq_ignore_ascii_case(required));
        if allows_none {
            if !output.ends_with('\n') {
                output.push('\n');
            }
            output.push_str(&format!("\n## {}\nNone\n", required));
            result.changes.push(format!("added: ## {} (None)", required));
        }
    }

    // Step 4: Add missing optional allow_none sections (not required, not conditional)
    // Conditional sections (e.g. Flows) are handled separately in Step 5 with context check.
    if doc_type == "developers_md" {
        let sections = self.parse_sections(&output);
        for optional in DEVELOPERS_ALLOW_NONE_SECTIONS {
            if DEVELOPERS_REQUIRED_SECTIONS.iter().any(|r| r.eq_ignore_ascii_case(optional)) {
                continue; // already handled in Step 3
            }
            // Skip conditional sections — their addition is context-dependent (Step 5)
            if DEVELOPERS_CONDITIONAL_SECTIONS.iter().any(|(name, _)| name.eq_ignore_ascii_case(optional)) {
                continue;
            }
            let found = sections.iter().any(|s| s.name.eq_ignore_ascii_case(optional));
            if !found {
                if !output.ends_with('\n') {
                    output.push('\n');
                }
                output.push_str(&format!("\n## {}\nNone\n", optional));
                result.changes.push(format!("added: ## {} (None)", optional));
            }
        }
    }

    // Step 5: Add conditional sections when context condition is met
    if doc_type == "developers_md" {
        if let Some(ctx) = ctx {
            let sections = self.parse_sections(&output);
            for (name, condition) in DEVELOPERS_CONDITIONAL_SECTIONS {
                let found = sections.iter().any(|s| s.name.eq_ignore_ascii_case(name));
                if found {
                    continue;
                }
                if Self::condition_met(condition, ctx) {
                    if !output.ends_with('\n') {
                        output.push('\n');
                    }
                    output.push_str(&format!("\n## {}\nNone\n", name));
                    result.changes.push(format!("added: ## {} (None, condition: {})", name, condition));
                }
            }
        }
    }

    result.content = output;
    result
}
```

- [ ] **Step 5: main.rs FixSchema handler에 context 전달**

`main.rs`의 FixSchema 핸들러를 다음으로 수정:

```rust
Commands::FixSchema { file, output, doc_type, dry_run } => {
    match std::fs::read_to_string(&file) {
        Ok(content) => {
            let validator = SchemaValidator::new();
            // Compute context from file's parent directory for conditional sections
            let ctx = file.parent().map(|d| {
                claude_md_core::schema_validator::SchemaValidator::evaluate_conditions(d)
            });
            let converge_result = validator.converge_schema_with_context(&content, doc_type, ctx.as_ref());
            // ... (이하 기존 코드 그대로)
```

- [ ] **Step 6: 테스트 통과 확인**

```bash
cd core && cargo test test_converge_developers_md 2>&1 | tail -15
```

Expected: 모든 test_converge_developers_md_* 테스트 `ok`

- [ ] **Step 7: 커밋**

```bash
git add core/src/schema_validator.rs core/src/main.rs
git commit -m "feat(schema_validator): converge_schema_with_context — optional+conditional sections skeleton 추가"
```

---

### Task 3: 경고 메시지 하드코딩 수정 (1줄 버그)

**Files:**
- Modify: `core/src/schema_validator.rs` (~line 301)

**배경:**
`DEVELOPERS_CONDITIONAL_SECTIONS` 루프에서 `(name, condition)` 쌍을 순회하지만
경고 메시지에 `name` 변수 대신 "Flows"를 하드코딩. 향후 다른 conditional section 추가 시 오류.

- [ ] **Step 1: 다중 conditional section 경고 테스트 작성 (RED)**

`schema_validator.rs` 테스트 블록에 추가. 이 테스트는 현재 DEVELOPERS_CONDITIONAL_SECTIONS에 Flows만 있어 즉시 실패하지 않지만 경고 메시지 내용을 검증:

```rust
#[test]
fn test_flows_warning_mentions_section_name() {
    let content = r#"# Test Module

## Constraints
None

## Technical Context
None

## Flows

### Login
1. api/auth — POST /login
"#;
    let validator = SchemaValidator::new();
    let ctx = ValidationContext { is_project_root: false, is_module_root: false };
    let result = validator.validate_developers_with_context(
        // needs a file path — use tempfile
        // 이 테스트는 validate_strict_with_context를 통해 간접 검증
        std::path::Path::new("dummy"),
        Some(&ctx),
    );
    // Result will be Err (file not found), but we test warning content via cucumber
}
```

이 경우 unit test보다 cucumber scenario가 더 적합 — Task 5에서 cucumber로 커버.

- [ ] **Step 2: 경고 메시지 수정**

`schema_validator.rs`의 conditional section 경고 블록 (~line 298-307):

**현재:**
```rust
warnings.push(format!(
    "Flows section is only expected at project root (condition: {}). \
     Move to project root DEVELOPERS.md or remove.",
    condition
));
```

**수정:**
```rust
warnings.push(format!(
    "'{}' section is only expected under condition '{}'. \
     Move to project root DEVELOPERS.md or remove.",
    name, condition
));
```

- [ ] **Step 3: 컴파일 + 기존 테스트 통과 확인**

```bash
cd core && cargo test 2>&1 | tail -10
```

Expected: `test result: ok`

- [ ] **Step 4: Cucumber — 경고 메시지 내용 검증**

`tests/features/developers_md_validator.feature`에 시나리오 추가:

```gherkin
Scenario: Flows warning message mentions section name not hardcoded string
  Given CLAUDE.md with content:
    """
    # Test Module

    ## Purpose
    Test module.

    ## Requirements
    None

    ## Domain Context
    None
    """
  And DEVELOPERS.md with content:
    """
    # Test Module

    ## Constraints
    None

    ## Technical Context
    None

    ## Flows

    ### Login
    1. api/auth — POST /login
    """
  When I validate the schema with strict mode in non-project-root
  Then validation should pass
  And validation should have warnings
  And warning should mention "Flows"
  And warning should mention "is_project_root"
```

- [ ] **Step 5: Cucumber 테스트 통과 확인**

```bash
cd core && cargo test --test cucumber 2>&1 | grep -E "passed|failed|skipped" | tail -5
```

Expected: 새 시나리오 포함 전체 passed 수 증가

- [ ] **Step 6: 커밋**

```bash
git add core/src/schema_validator.rs core/tests/features/developers_md_validator.feature
git commit -m "fix(schema_validator): Flows 경고 메시지 하드코딩 → name 변수 사용"
```

---

### Task 4: analysis_formatter에 format_data_schemas 추가

**Files:**
- Modify: `core/src/analysis_formatter.rs`

**배경:**
설계 명세: `/decompile → format-analysis → Data Schemas 자동 생성 (ExportedType 데이터 활용)`.
현재 `format_analysis`는 `## Exports` 섹션에 ExportedType을 출력하지만,
decompiler agent가 이를 Data Schemas 형식으로 해석하기 위한 명시적 섹션이 없다.
`## Data Schemas (Detected)` 섹션을 추가하여 agent가 일관된 DEVELOPERS.md를 생성할 수 있게 한다.

ExportedType의 `definition` 필드에 구조 정보가 있고, ExportedEnum의 `variants`에 열거값이 있다.
definition/variants가 없는 타입은 이름만 출력하고 agent에게 채우도록 힌트를 준다.

- [ ] **Step 1: format_data_schemas 테스트 작성 (RED)**

`analysis_formatter.rs`의 `mod tests` 블록에 추가. `make_empty_analysis()` 헬퍼는 기존에 있음:

```rust
#[test]
fn test_format_data_schemas_with_types_and_enums() {
    use crate::code_analyzer::{ExportedEnum, ExportedType, TypeKind};
    let mut analysis = make_empty_analysis();
    analysis.exports.types.push(ExportedType {
        name: "Claims".to_string(),
        kind: TypeKind::Interface,
        definition: Some("userId: string, exp: number".to_string()),
        description: None,
    });
    analysis.exports.enums.push(ExportedEnum {
        name: "AuthError".to_string(),
        variants: Some(vec!["EXPIRED".to_string(), "INVALID_SIGNATURE".to_string()]),
    });

    let result = format_analysis(&analysis);

    assert!(result.contains("## Data Schemas (Detected)"), "Missing Data Schemas section");
    assert!(result.contains("Claims"), "Missing type name");
    assert!(result.contains("userId: string"), "Missing type definition");
    assert!(result.contains("AuthError"), "Missing enum name");
    assert!(result.contains("EXPIRED"), "Missing enum variant");
}

#[test]
fn test_format_data_schemas_omitted_when_no_types() {
    let analysis = make_empty_analysis();
    // exports.types and exports.enums are empty

    let result = format_analysis(&analysis);

    assert!(!result.contains("Data Schemas"), "Data Schemas should be omitted when no types");
}

#[test]
fn test_format_data_schemas_type_without_definition() {
    use crate::code_analyzer::{ExportedType, TypeKind};
    let mut analysis = make_empty_analysis();
    analysis.exports.types.push(ExportedType {
        name: "TokenPair".to_string(),
        kind: TypeKind::Type,
        definition: None,
        description: None,
    });

    let result = format_analysis(&analysis);

    assert!(result.contains("## Data Schemas (Detected)"));
    assert!(result.contains("TokenPair"));
    assert!(result.contains("type"));
}
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
cd core && cargo test test_format_data_schemas 2>&1 | tail -5
```

Expected: `FAILED` (Data Schemas section not in output)

- [ ] **Step 3: format_data_schemas 함수 구현**

`analysis_formatter.rs`에 함수 추가 (format_env_vars 함수 바로 다음에):

```rust
/// Formats exported types and enums as a Data Schemas section for LLM consumption.
/// The "(Detected)" prefix signals auto-extracted content.
/// The decompiler agent maps this to ## Data Schemas in DEVELOPERS.md.
fn format_data_schemas(
    types: &[crate::code_analyzer::ExportedType],
    enums: &[crate::code_analyzer::ExportedEnum],
) -> Option<String> {
    if types.is_empty() && enums.is_empty() {
        return None;
    }

    let mut lines = vec!["## Data Schemas (Detected)".to_string()];

    let mut sorted_types: Vec<&crate::code_analyzer::ExportedType> = types.iter().collect();
    sorted_types.sort_by(|a, b| a.name.cmp(&b.name));

    for t in sorted_types {
        let kind_str = format!("{:?}", t.kind).to_lowercase();
        lines.push(String::new());
        match &t.definition {
            Some(def) if !def.is_empty() => {
                lines.push(format!("### {} ({})", t.name, kind_str));
                lines.push(String::new());
                lines.push(format!("```\n{}\n```", def));
            }
            _ => {
                lines.push(format!("### {} ({}) — definition not extracted", t.name, kind_str));
            }
        }
    }

    let mut sorted_enums: Vec<&crate::code_analyzer::ExportedEnum> = enums.iter().collect();
    sorted_enums.sort_by(|a, b| a.name.cmp(&b.name));

    for e in sorted_enums {
        lines.push(String::new());
        match &e.variants {
            Some(variants) if !variants.is_empty() => {
                lines.push(format!("### {} (enum)", e.name));
                lines.push(String::new());
                lines.push(format!("Variants: {}", variants.join(" | ")));
            }
            _ => {
                lines.push(format!("### {} (enum) — variants not extracted", e.name));
            }
        }
    }

    Some(lines.join("\n"))
}
```

- [ ] **Step 4: format_analysis에 Data Schemas 섹션 추가**

`analysis_formatter.rs`의 `format_analysis` 함수에서 `// Environment Variables` 블록 바로 위에 삽입:

```rust
// Data Schemas (public types detected from source code)
if let Some(s) = format_data_schemas(&analysis.exports.types, &analysis.exports.enums) {
    sections.push(s);
}

// Environment Variables (detected from source code)
```

`use` 문에 `ExportedEnum`, `ExportedType`이 이미 없다면 `analysis_formatter.rs` 상단 import 확인:
현재 import: `use crate::code_analyzer::{AnalysisResult, Behavior, BehaviorCategory, Contract, Dependencies, FunctionContract, InternalDependency, Protocol, ResolutionStatus};`
→ ExportedType/ExportedEnum은 `crate::code_analyzer::ExportedType` 방식으로 직접 참조하므로 import 변경 불필요.

- [ ] **Step 5: 테스트 통과 확인**

```bash
cd core && cargo test test_format_data_schemas 2>&1 | tail -10
```

Expected: 3개 테스트 모두 `ok`

- [ ] **Step 6: 전체 테스트 통과 확인**

```bash
cd core && cargo test 2>&1 | tail -5
```

Expected: `test result: ok`

- [ ] **Step 7: 커밋**

```bash
git add core/src/analysis_formatter.rs
git commit -m "feat(analysis_formatter): format_data_schemas 추가 — ExportedType/Enum → Data Schemas 섹션"
```

---

### Task 5: Cucumber — is_project_root 자동 감지 + Flows 경고 수정 검증

**Files:**
- Modify: `core/tests/features/developers_md_validator.feature`
- Modify: `core/tests/cucumber.rs` (새 step 추가)

**배경:**
Task 1에서 수정한 `is_project_root` 자동 감지를 cucumber로 검증.
현재 "non-project-root" step만 있고 "project-root" step(`.git` 있는 환경)이 없다.
Flows가 project root에 있을 때 경고가 발생하지 않아야 함을 end-to-end로 검증.

- [ ] **Step 1: Feature 시나리오 추가**

`tests/features/developers_md_validator.feature` 끝에 추가:

```gherkin
Scenario: Flows section in project-root DEVELOPERS.md does not generate warning
  Given CLAUDE.md with content:
    """
    # Project Root

    ## Purpose
    Project root module.

    ## Requirements
    None

    ## Domain Context
    None
    """
  And DEVELOPERS.md with content:
    """
    # Project Root

    ## Constraints
    None

    ## Technical Context
    None

    ## Flows

    ### User Login
    1. api/auth — POST /login
    2. domain/auth — validateCredentials() → Session
    """
  When I validate the schema with strict mode in project-root
  Then validation should pass
  And validation should have no warnings about "Flows"
```

- [ ] **Step 2: cucumber.rs에 step 구현 추가**

`cucumber.rs`에서 기존 `validate_schema_strict_non_root` step 근처에 추가:

```rust
#[when("I validate the schema with strict mode in project-root")]
fn validate_schema_strict_project_root(world: &mut TestWorld) {
    use claude_md_core::schema_validator::{SchemaValidator, ValidationContext};
    let claude_md_path = world.claude_md_paths.get("root").expect("No CLAUDE.md path");
    let ctx = ValidationContext {
        is_project_root: true,
        is_module_root: false,
    };
    let validator = SchemaValidator::new();
    world.validation_result = Some(validator.validate_strict_with_context(claude_md_path, Some(&ctx)));
}
```

그리고 새 Then step:

```rust
#[then(regex = r#"validation should have no warnings about "(.+)""#)]
fn validation_no_warnings_about(world: &mut TestWorld, keyword: String) {
    let result = world.validation_result.as_ref().expect("No validation result");
    let matching: Vec<&String> = result.warnings.iter()
        .filter(|w| w.contains(&keyword))
        .collect();
    assert!(
        matching.is_empty(),
        "Expected no warnings about '{}', but found: {:?}", keyword, matching
    );
}
```

- [ ] **Step 3: Cucumber 테스트 통과 확인**

```bash
cd core && cargo test --test cucumber 2>&1 | grep -E "Scenario|passed|failed" | tail -10
```

Expected: 새 시나리오 `passed`

- [ ] **Step 4: 전체 테스트 최종 확인**

```bash
cd core && cargo test 2>&1 | tail -5
```

Expected: `test result: ok. N passed`

- [ ] **Step 5: 커밋**

```bash
git add core/tests/features/developers_md_validator.feature core/tests/cucumber.rs
git commit -m "test: is_project_root 자동 감지 + Flows 경고 수정 cucumber 검증 추가"
```

---

## Self-Review

### Spec 커버리지

| 수정 항목 | Task |
|-----------|------|
| evaluate_conditions .git 감지 | Task 1 ✓ |
| converge_schema optional sections | Task 2 ✓ |
| converge_schema conditional sections (Flows) | Task 2 ✓ |
| main.rs FixSchema context 전달 | Task 2 ✓ |
| 경고 메시지 하드코딩 수정 | Task 3 ✓ |
| format_data_schemas | Task 4 ✓ |
| Cucumber 검증 | Task 5 ✓ |

### 타입 일관성

- `ValidationContext` — Task 1, 2, 5 모두 `{ is_project_root: bool, is_module_root: bool }` 일관
- `converge_schema_with_context` 시그니처 — Task 2 구현과 Task 2 테스트 일치
- `format_data_schemas` 인자 `&[ExportedType], &[ExportedEnum]` — Task 4 구현/테스트 일치

### 주의사항

- Task 2 Step 4의 `DEVELOPERS_ALLOW_NONE_SECTIONS`에 Data Schemas가 포함되어 있는지 확인 필요.
  schema-rules.yaml에서 `data_schemas: required: false, allow_none: true`이므로 build.rs가 이를 `DEVELOPERS_ALLOW_NONE_SECTIONS`에 포함시켜야 함.
  build.rs 확인: `filter(|s| s.allow_none)` 조건으로 수집하므로 포함됨.
- Task 4의 `analysis_formatter.rs` import — `ExportedType`/`ExportedEnum`을 `crate::code_analyzer::` prefix로 참조하므로 별도 import 불필요.
