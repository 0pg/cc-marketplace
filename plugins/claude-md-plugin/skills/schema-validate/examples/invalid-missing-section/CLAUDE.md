# src/auth

## Purpose

JWT 기반 인증 처리 모듈.

## Exports

| Name | Signature | Description |
|------|-----------|-------------|
| `validateToken` | `(token: string): Promise<Claims>` | JWT 토큰 검증 |

<!-- Missing Behavior section - will fail validation -->
